[package]
name = "field_unpacker"
version = "0.1.0"
edition = "2021"

[dependencies]
regex = "^1.8"
walkdir = "^2.3"
clap = { version = "^4.0", features = ["derive"] }
once_cell = "^1.20.2"
chrono = "^0.4"

[build]
rustflags = ["-C", "target-feature=+crt-static"]
